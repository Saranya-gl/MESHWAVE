import 'package:flutter/material.dart';

class AddDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Details'),
      ),
      body: Center(
        child: Text('Add your details here.'),
      ),
    );
  }
}
